version: '3.8'
services:
  wa-gateway:
    image: localhost/wa-gateway:latest
    build: .
    container_name: wa-gateway
    restart: always
    tty: true
    stdin_open: true
    ports:
      - "3000:3000"
    env_file:
      - .env
    volumes:
      - ./auth_info:/app/auth_info
    logging:
      driver: "json-file"
      options:
        max-size: "10m"
        max-file: "3"