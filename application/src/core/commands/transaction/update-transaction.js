import { updateTransaction } from "../../action/transaction/update-transaction.js";

export const UPDATE_TRANSACTION_COMMAND = {
    name: 'transaction_update',
    description: 'Update transaction',
    points: 2,
    execute: (services) => async (message) => {
        
    }
}